version: 2
jobs:
  build:
    docker:
      - image: circleci/node:4-browsers
    steps:
      - checkout
      - run: echo "hello world"
  test:
    docker:
      - image: circleci/node:4-browsers
    steps:
      - checkout
      - run: yarn run eslint .
